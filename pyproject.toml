[project]
name = "build-asteroids"
version = "0.1.0"
description = "My attempt at building the Asteroids project on Boot.dev"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
